<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Form Page</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
</head>

<body>
    <div class="container d-flex align-items-center justify-content-center">
        <div class="w-25 ">
            <h2 class="pb-4 text-center">Add Record</h2>
            <form action="/updateRecord" method="post">
                <input type="text" name="id" value="<%= editData?.id %>" class="form-control mb-3"/>
                <div class="mb-3">
                    Task:
                    <input type="text" name="usertask" class="form-control" value="<%= editData?.task %>"/>
                </div>
                <div class="mb-3">
                    Status:
                    <!-- <input type="text" name="userage" class="form-control" /> -->
                    <select name="userstatus" id="" class="form-select">
                        <option selected value="">--Select Status--</option>
                        <option value="pending" <%= editData?.Status === 'pending' ? 'selected' : '' %> >pending</option>
                        <option value="complete" <%= editData?.Status === 'complete' ? 'selected' : '' %>>complete</option>
                        <option value="ongoing" <%= editData?.Status === 'ongoing' ? 'selected' : '' %>>ongoing</option>
                    </select>
                </div>
                <div class="mb-3">
                    DaedLine:
                    <input type="date" name="userdate" class="form-control" value="<%= editData?.date %>"/>
                </div>
                <div class="d-flex justify-content-center">
                    <input type="submit" value="submit" class="btn btn-primary px-3">
                    <a href="/" class="btn btn-primary px-4 ms-3">View</a>
                </div>
            </form>

        </div>
    </div>
</body>

</html>